// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['img.freepik.com'], // Add the allowed domain here
    },
  };

  export default nextConfig;
